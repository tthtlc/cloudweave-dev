
read nutanix_bastion_internal_server.md  and aim to produce a script that can generate 2VM as according to that scenario in nutanix_bastion_internal_server.md:   one is bastion host, and another is internal private server (with no access to internet).   it should be executed by a button "Provision Private VM Machine" beside the existing "Provision Nutanix".   only Nutanix owner and admin role can execute the script or have that button displayed.   And during execution all the relevant credentials from Vault should be automatically picked as according to the tenant details.   

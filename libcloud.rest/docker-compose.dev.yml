services:
  api:
    volumes:
      - .:/app
      - api-data:/app/data
    command:
      - uvicorn
      - app.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8765"
      - --reload

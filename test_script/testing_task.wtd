write a script to execute "bash myrun_aws_admin.sh $1" where $1 will range through all the files (as long as it is non-nutanix related files) inside the entire directory of ./scripts (except for deprovision_aws.sh and deprovision_nutanix.sh) and report on the output of the execution (success or error and what the errors).    ensure that when executed, only 5 random scripts are chosen to be executed, and all executed output has stdout and stderr output into different files in a tmporary logging output directory.

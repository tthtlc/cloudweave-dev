# Dex standalone project — environment overrides.
# Copy to .env and adjust as needed. setup.sh creates the shared libcloud_net
# network and renders config.yaml from config.template.yaml before starting Dex.

# Host port published for Dex's HTTP listener (OIDC discovery, auth, token).
DEX_HTTP_PORT=5556
